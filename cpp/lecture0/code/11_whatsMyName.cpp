#include <iostream>
#include <string>

using namespace std;

int main() {
    /**
     * Problem 11
     * If the user writes
     * "cpp" => program replies "That's right!!"
     * "c" => program replies "It's my younger brother..."
     * "java" => program replies "What a mess!!"
     * "javascript" => program replies "Too inefficient languages!!"
     * other input => program replies "I never heard of it"
     * 
     * Program should repeatedly ask it's name until the user types "cpp"
     * Limitation: use switch, while, break statements
     */

    string name;
    cout << "What's my name?? ";
    cin >> name;

    return 0;
}
