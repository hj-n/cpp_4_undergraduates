#include <iostream>

using namespace std;

void multiplyMatrix(int (*matrixA)[2], int (*matrixB)[2], int (*result)[2]) {
    /**
     * You can access the value of each variable using a[i][j] syntax
     * In this function, you need to multiply matrixA and matrixB and store
     * the result in "result" variable.
     * Notice that you should also pass the storage to store "result"
     * ex)
       int a[2][2] = {{1, 2}, {3, 4}};
       int b[2][2] = {{1, 2}, {3, 4}};
       int result[2][2];
       multiplyMatrix(a, b, result)
       // In this moment, "result" will have:
       // {{7 , 10},
       //   15, 22}}
      
     */ 
}

int main() {
    /**
     * Problem 16
     * INPUT: natural number n
     * OUTPUT: n-th fibonacci number
     * Limitation: use matrix method
     * store each matrix in [2][2] array
     * implement and use the funtion which multiplies two matrix
     */
}