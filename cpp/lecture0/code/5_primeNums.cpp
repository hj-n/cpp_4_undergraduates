#include <iostream>
#include <cmath>  // new library!!

using namespace std;

int main() {
    /**
     * Problem 4
     * INPUT : any integer n
     * OUTPUT : every prime number from 1 to n
     * hint : use the statement: if an integer n cannot be divied by integers
     *        2 <= x <= n^(0.5), then the integer is a prime number
     * hint : use sqrt() function in cmath library
     */

    
    return 0;
}